import Component from '../../lib/Component.js';

export default class SummaryDetail extends Component {
  template() {
    return `
      <div id="summary-details" class="space-y-3">
        <!-- Divider가 필요한 곳에 동적으로 추가될 예정 -->
      </div>
    `;
  }
}
